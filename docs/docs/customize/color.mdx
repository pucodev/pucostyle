import ColorSwatch from '../../src/components/ColorSwatch.mdx'
import { colors } from '../../src/js/variables'

# Colores

Se maneja un conjunto de colores en variables css usando el patron `--ps-color-{color-name}`

## Tabla de colores

export const ColorRow = ({ color }) => (
  <tr>
    <td>{color}</td>
    <td>
      <ColorSwatch color={color}></ColorSwatch>
    </td>
    <td>
      <code>--ps-color-{color}</code>
    </td>
  </tr>
)

<table>
  <thead>
    <tr>
      <th>Color</th>
      <th>Preview</th>
      <th>Variable</th>
    </tr>
  </thead>
  <tbody>
    {colors.map((color, idx) => (
      <ColorRow color={color} />
    ))}
  </tbody>
</table>
